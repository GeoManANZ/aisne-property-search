# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Browsers
.browsers/

# Scan artifacts (large HTML dumps)
scans/

# Images/screenshots
*.png

# Secrets — NEVER commit API keys
.env

# Session cookies / captures (contain live DataDome tokens)
cookies.json
seloger_pages/
seloger_data/

# Local DB (committed as schema, not data)
# listings.db
*.html
engine_metrics.jsonl
listings.db
aisne-recommendations-weekly.md
logs/*.log

# DVF downloads (~4 MB) + liveness baselines — data, not code
data/dvf/
data/*.json
listings.db-shm
listings.db-wal
